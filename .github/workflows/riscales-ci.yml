name: CI Riscales Bot — Frontend & Backend

on:
  pull_request:
    branches:
      - main

jobs:
  frontend:
    name: Validaciones Frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend

    steps:
      - name: Checkout repositorio
        uses: actions/checkout@v3

      - name: Configurar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Instalar pnpm
        run: npm install -g pnpm

      - name: Instalar dependencias frontend
        run: pnpm install

      - name: Lint frontend
        run: pnpm lint

      - name: Test frontend
        run: pnpm test

      - name: Build frontend
        run: pnpm build

  backend:
    name: Validaciones Backend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend

    steps:
      - name: Checkout repositorio
        uses: actions/checkout@v3

      - name: Configurar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Instalar pnpm
        run: npm install -g pnpm

      - name: Instalar dependencias backend
        run: pnpm install

      - name: Lint backend
        run: pnpm lint

      - name: Test backend
        run: pnpm test

  cleanup:
    name: Cleanup temporal (opcional)
    runs-on: ubuntu-latest
    steps:
      - name: Mostrar estructura del repo
        run: ls -la

